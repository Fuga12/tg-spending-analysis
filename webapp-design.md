# Дизайн веб-интерфейса

Дополнение к `webapp.md`: что именно на экранах и почему оно выглядит так.
`webapp.md` описывает механику (вход, API, права), этот документ — то, что
видит человек.

Реализовывать по фазам 8.2–8.4. Не выдумывать экранов сверх описанных.

---

## 1. Кому и зачем

Двое, общий бюджет. Записывают тратами боту с телефона, на бегу. Сайт
открывают тоже с телефона — значит, **мобильный экран первичен**, десктоп
получается растяжением, а не наоборот.

Задачи, по убыванию частоты:

1. **«Сколько ушло и на что»** — понять за пять секунд после открытия.
2. **«Исправить вот эту запись»** — опечатка в сумме, не та категория, не тот
   день. В боте сумму не исправить вообще: ради этого сайт и затевается.
3. **«Что бот не понял»** — записи с `needs_classification`. У бота такого
   экрана нет, а разгребать их надо.
4. **«Кто из нас сколько платит»** — не для взаиморасчётов (их не считаем,
   `plan.md` §10), а чтобы видеть перекос.
5. **«Добавить руками»** — задним числом, без бота.

Всё остальное — не задача. Ни бюджетов по категориям, ни целей, ни прогнозов.

---

## 2. Экраны и переходы

Три состояния, все на одном URL с хэшем:

| Хэш | Что |
|---|---|
| `#/m/2026-07` | Месяц: сводка, графики, список операций |
| `#/m/2026-07/t/482` | То же плюс открытая карточка операции |
| `#/m/2026-07/new` | То же плюс форма добавления |

Карточка — не отдельная страница, а слой поверх месяца: на телефоне лист
снизу, на десктопе модалка шириной до 560px. Кнопка «назад» в браузере
закрывает лист, а не уводит с месяца — поэтому хэш меняется, а данные месяца
не перезапрашиваются.

Фильтры живут в хэше запросом: `#/m/2026-07?cat=Продукты`. Значит, отфильтрованный
вид можно переслать ссылкой, а «назад» снимает фильтр.

Никаких вкладок, бургеров и нижней навигации: экранов по сути два, а
переключатель на два пункта — это шум.

---

## 3. Экран месяца, блок за блоком

Одна вертикальная лента. Сверху — то, ради чего открыли, ниже — детали.
Ширина контента ограничена 720px и центрируется: на десктопе список во весь
монитор нечитаем.

### 3.1 Шапка (прилипает)

```
‹     Июль 2026     ›
```

При скролле сжимается в одну строку: слева «Июль», справа итог мелким —
контекст не теряется, когда крупная сумма уехала вверх. Свайп влево-вправо
листает месяц. Будущие месяцы недоступны: стрелка «›» гаснет на текущем.

### 3.2 Итог месяца — главное число

Одно на весь экран, ≥48px, тот же системный шрифт, что везде, цифры
пропорциональные (табличные на таком кегле выглядят разреженно).

```
Всего за июль
68 400 ₽
↑ 6% к июню · за первые 28 дней
```

**Сравнение честное.** Неполный месяц сравнивается с тем же отрезком
прошлого: 1–28 июля против 1–28 июня. Иначе третьего числа интерфейс
показывал бы «−90%», и это враньё. Подпись «за первые 28 дней» появляется,
только пока месяц не кончился.

**Дельта нейтральная.** Стрелка и знак несут смысл, цвет — нет. Красить рост
трат в тревожный красный каждый месяц значит превратить учёт в надзирателя;
у роста бывает причина — отпуск, ремонт, зубной. Цветом отмечаем факты, а не
оценки.

### 3.3 Шесть месяцев подряд

Спарклайн из шести точек под итогом: текущий месяц акцентным цветом,
остальные — приглушённым. Тап по точке уводит на тот месяц. Оси нет,
подписей нет — это форма, а не таблица.

### 3.4 Кто платил и на кого ушло

Две полосы 100%-стека, одна под другой:

```
Кто платил
[■■■■■■■■■■■■■■ Илья 47 200 ][■■■■■■ Аня 21 200 ]

На кого ушло
[■■■■■ Илья 31 500 ][■■■ Аня 18 900 ][■■■■ Общее 18 000 ]
```

- Цвет закреплён за человеком навсегда: Илья — слот 1, Аня — слот 2, «Общее»
  и «Партнёру» — слот 3. Фильтр, меняющий состав, цвета не перекрашивает.
- Сегменты разделяет **зазор 2px цветом фона**, не обводка.
- Подпись и сумма — внутри сегмента, если помещаются с полями; иначе
  уезжают наружу. Обрезать подпись нельзя ни при каких условиях.
- Легенда есть всегда: две серии и больше — значит, опознавать по одному
  цвету человека не заставляем.

### 3.5 По категориям

Горизонтальные бары, отсортированы по убыванию. **Все бары одного цвета** —
это одна серия, и цвет здесь не значит ничего, кроме «это данные». Раскрасить
14 категорий в 14 оттенков — классическая ошибка: после седьмого цвета
соседние перестают различаться, а под дальтонизмом ломаются раньше.

```
Продукты          ████████████████  22 100 ₽  32%
Жильё             ████████████      18 000 ₽  26%
Доставка еды      ██████             9 300 ₽  14%
…
Остальное         ██                 3 100 ₽   5%
```

- Показываем восемь строк, хвост сворачивается в «Остальное» с раскрытием.
- Толщина бара ≤24px, конец данных скруглён на 4px, у базовой линии — прямой.
- Значение у кончика бара, процент следом приглушённым.
- Тап по строке фильтрует список ниже по этой категории (и пишется в хэш).

### 3.6 По дням

Столбики на всю ширину месяца. Одна серия — один цвет; сегодняшний столбик
акцентный. Подписи оси только 1, 8, 15, 22, 29 — остальное в подсказке при
наведении. Тап по столбику скроллит список к этому дню.

Смысл блока один: увидеть всплеск и вспомнить, что это было.

### 3.7 «Не разобрано»

Появляется, только если такие записи есть:

```
⚠ 3 записи без категории          Разобрать →
```

Тап фильтрует список по ним. Это единственное место, где интерфейс что-то
требует от человека, — поэтому оно и выглядит иначе, со статусным цветом,
иконкой и словами (цвет один смысла не несёт никогда).

### 3.8 Список операций

Сгруппирован по дням, свежие сверху. Заголовок дня — липкий, пока день на
экране:

```
Сегодня                                    2 340 ₽
  Пятёрочка                                1 200 ₽
  Продукты · на двоих                          И
  Такси                                      400 ₽
  Такси · на себя                              А
```

- Высота строки 56px — палец попадает без прицеливания.
- Слева описание крупно, под ним категория и «на кого» приглушённым.
- Справа сумма табличными цифрами, чтобы разряды стояли колонкой, под ней
  кружок с инициалом плательщика.
- Запись без категории — тонкая полоска статусного цвета слева и подпись
  «без категории» вместо категории.
- Перевод — «↔ Перевод», сумма приглушённая: в итог месяца он не входит,
  и выглядеть как трата не должен.
- Поступление — знак «+» перед суммой.

Тап по строке открывает карточку. **Свайпа-удаления нет**: смахнуть деньги
случайно нельзя, удаление живёт за подтверждением.

### 3.9 Кнопка «добавить»

Круглая, 56px, в правом нижнем углу, над безопасной зоной. Одна на весь
интерфейс — потому что действие ровно одно.

---

## 4. Карточка операции

На телефоне — лист снизу с ручкой, на десктопе — модалка. Открывается за
150мс, закрывается свайпом вниз, «назад» или крестиком.

```
─────
Трата

    1 200 ₽              ← крупное поле, цифровая клавиатура

Описание   [ пятёрочка            ]

Категория
[Продукты] [Кафе] [Доставка] [Транспорт]
[Такси] [Жильё] [Коммуналка] …

На кого   [ 👤 мне ][ 🧍 ей ][ 👥 нам ]

Тип       [ трата ][ перевод ][ доход ]

Когда     [ сегодня ][ вчера ][ 12.07 ]

из сообщения: «вчера взял в пятёрочке на 1200 и такси 400»

[        Сохранить        ]
              Удалить
```

- **Сумма — первое и самое крупное поле.** Ради неё сюда и приходят.
- **Категории — чипы, а не выпадающий список.** Четырнадцать штук видно
  целиком, выбор в один тап вместо трёх.
- **Тип «перевод»** прячет категорию и «на кого»: у перевода их не бывает
  (`plan.md` §3). Поля исчезают, а не гаснут, — иначе непонятно, чего ждать.
- **Исходное сообщение** внизу серым. Объясняет, откуда взялась сумма, и
  помогает понять, что бот не так понял.
- **Чужая запись** — те же поля, но неактивные, сверху плашка «Запись Ани».
  Не прятать: смотреть чужие траты можно, бюджет общий.

**Удаление**: подтверждение, потом тост «Удалено · Вернуть» на шесть секунд.
Удаление мягкое, вернуть — один запрос, а страх нажать кнопку исчезает.

**Сохранение оптимистичное**: список обновляется сразу, при ошибке
откатывается и показывает тост с причиной.

---

## 5. Как это выглядит

### 5.1 Поверхности

Тёмная тема основная — открывать это будут вечером с телефона. Светлая
обязательна и собирается из тех же ролей, а не инверсией.

| Роль | Тёмная | Светлая |
|---|---|---|
| Фон страницы | `#0d0d0d` | `#f9f9f7` |
| Карточка, лента | `#1a1a19` | `#fcfcfb` |
| Поднятый слой (лист, модалка) | `#232322` | `#ffffff` |
| Волосяная линия | `rgba(255,255,255,.10)` | `rgba(11,11,11,.10)` |
| Основной текст | `#ffffff` | `#0b0b0b` |
| Вторичный текст | `#c3c2b7` | `#52514e` |
| Приглушённый (оси, подписи) | `#898781` | `#898781` |

Чистый чёрный не используем: на OLED он даёт смазывание при скролле и
проваливает границы карточек.

### 5.2 Цвет данных

Три слота, больше не нужно — и это проверено, а не на глаз:

| Слот | Кто | Тёмная | Светлая |
|---|---|---|---|
| 1 | первый участник, все одиночные графики | `#3987e5` | `#2a78d6` |
| 2 | второй участник | `#d95926` | `#eb6834` |
| 3 | «Общее», «Партнёру» | `#199e70` | `#1baf7a` |

Прогнано `validate_palette.js` в обоих режимах на всех парах: диапазон
светлоты, порог насыщенности, различимость при дальтонизме (худшая пара
ΔE 9.2 светлая / 9.4 тёмная при пороге 8) и контраст к фону — всё зелёное.
Единственное замечание: в светлой теме слот 3 даёт контраст 2.74 к фону,
поэтому у полос со «Общим» **всегда есть прямые подписи** — цвет не остаётся
единственным носителем смысла.

Статусные цвета (`#fab219` предупреждение, `#d03b3b` критично) — только для
состояний, никогда как серия, и всегда со значком и словами.

Акцент интерфейса — слот 1. Кнопки, выбранные чипы, активная дата.

### 5.3 Шрифт и цифры

Системный `system-ui, -apple-system, "Segoe UI", sans-serif` — ничего не
качаем, значит нечему тормозить и нечего ждать.

| Что | Размер / вес |
|---|---|
| Итог месяца | 48 / 600, пропорциональные цифры |
| Заголовок экрана, суммы дня | 20 / 600 |
| Строка списка, поля | 16 / 500 |
| Категория, «на кого», подписи | 13 / 400 |
| Оси графиков | 12 / 400 |

Табличные цифры (`tabular-nums`) — в колонке сумм и на осях, где разряды
обязаны стоять друг под другом. На крупных числах их не включаем: они
разъезжаются.

**Капса нет нигде.** Кириллица в верхнем регистре читается заметно хуже
латиницы, а «ПРОДУКТЫ» ещё и кричит.

### 5.4 Размеры и ритм

Шкала 4 / 8 / 12 / 16 / 24 / 32 / 48. Поля экрана 16 на телефоне, 24 на
десктопе. Скругления: 16 у карточек, 12 у полей и кнопок, полное у чипов.
Тач-цели не меньше 44×44, кнопки в листе — 48 в высоту. Нижний отступ
уважает `env(safe-area-inset-bottom)`, иначе на айфоне «Сохранить» уедет под
системную полосу.

Теней в тёмной теме нет — их там не видно; слои разделяются светлотой
поверхности и волосяной линией. В светлой поднятый слой получает мягкую тень.

### 5.5 Движение

Только два вида: появление листа снизу (200мс) и смена содержимого при
переключении месяца (150мс, растворение). Всё — `ease-out`.
`prefers-reduced-motion` выключает оба.

Чего не делаем: анимированного счёта цифр, параллакса, пружин, скелетонов,
мигающих при каждом перезапросе.

---

## 6. Правила графиков

Единые для всех трёх, чтобы они читались как один набор, а не три разных
виджета:

- **Толщина марок:** бар ≤24px, линия 2px, точка ≥8px.
- **Сетка и оси:** сплошные волосяные линии на шаг от фона. Пунктира нет —
  он читается как «прогноз».
- **Разделение:** зазор 2px цветом фона между сегментами стека и соседними
  барами. Обводок вокруг марок нет.
- **Подписи выборочные:** значение у кончика бара и у сегодняшнего столбика.
  Число над каждой точкой — это шум, который не читают.
- **Текст никогда не красится в цвет данных.** Опознание даёт цветная метка
  рядом, а не окрашенная надпись.
- **Подсказка при касании** есть у каждого графика: день, сумма, доля. Но она
  дополняет — любое значение доступно и без неё, из списка ниже.
- **Таблица-двойник:** блок «по категориям» и так таблица по сути; у графика
  по дням раскрывается список дней с суммами. Ни одно число не заперто
  внутри картинки.

Чего в интерфейсе не будет: пончиков и круговых диаграмм (сравнивать доли
на глаз по секторам люди не умеют), двух осей Y на одном графике, радуги
вместо шкалы, 3D.

---

## 7. Состояния, без которых интерфейс сырой

| Состояние | Что показываем |
|---|---|
| Первая загрузка | Скелет блоков — форма будущего экрана, не спиннер |
| Смена месяца | Прошлый экран приглушается до 60%, layout не прыгает |
| Пустой месяц | «В июле трат нет» и напоминание формата: «600 лимонад» |
| Нет связи | Плашка сверху: «Сервер не отвечает · Повторить» |
| Сессия истекла (401) | Экран «Сессия закончилась. Напиши боту /вход» |
| Запись удалили, пока правил (404) | «Этой записи больше нет» и закрытие карточки |
| Ошибка сохранения | Откат значения и тост с причиной |
| Чужая запись | Плашка «Запись Ани», поля неактивны |

---

## 8. Доступность

- Контраст текста ≥4.5:1 в обеих темах; крупного — ≥3:1.
- Смысл никогда не только в цвете: у статуса есть значок и слово, у сегментов
  графика — подпись, у «на кого» — иконка и текст.
- Фокус виден на всём, что кликается; Tab обходит экран в порядке чтения.
- Лист снизу — ловушка фокуса, Esc закрывает.
- `prefers-reduced-motion` уважается.
- Никаких подсказок только по наведению: на телефоне наведения нет.

---

## 9. Чем и как это писать

React 18 + Vite + TypeScript, как решено. Сверх этого:

- **Графиков-библиотек нет.** Три графика — бары, стек, спарклайн — это
  40–60 строк SVG каждый. `recharts` (упомянутый в `webapp.md` §5) добавил бы
  ~120 КБ к бандлу и всё равно потребовал бы переопределять каждый стиль под
  правила §6. Это осознанная замена решения из `webapp.md`.
- **CSS-фреймворка нет.** Переменные из §5.1–5.4 в одном файле, дальше
  обычный CSS-модуль на компонент. Стилей тут на пару сотен строк.
- **Менеджера состояния нет.** `useState` плюс тонкая обёртка над `fetch`,
  которая умеет 401 (увести на экран сессии) и откат.
- **Роутер свой** — тридцать строк на разбор хэша. Полноценный роутер ради
  трёх состояний — лишняя зависимость.

Бандл-потолок: **60 КБ gzip** без React, 110 КБ с ним. Больше — значит,
что-то приехало лишнее, идти смотреть.

---

## 10. Что API должен уметь сверх `webapp.md` §4

- `GET /api/report/months?n=6` — суммы по месяцам для спарклайна.
- `POST /api/transactions/{id}/restore` — снять мягкое удаление, для «Вернуть».
- В ответе списка — счётчик `pending` (записи без категории), чтобы блок §3.7
  не требовал второго запроса.

---

## 11. Приёмка

Фаза не закрыта, пока не проверено руками:

1. Ширина окна 360px: горизонтального скролла нет, ничего не обрезано.
2. Суммы в списке стоят колонкой, разряд под разрядом.
3. Переключение месяца не дёргает layout и не мигает белым.
4. Светлая тема не «инверсия»: контрасты держатся, графики читаются.
5. `validate_palette.js` зелёный в обоих режимах (прогнан, см. §5.2).
6. `prefers-reduced-motion` гасит анимации.
7. Клавиатура: Tab доходит до всех действий, фокус виден, Esc закрывает лист.
8. Бандл в пределах §9.
9. Открыть на телефоне и записать/исправить трату, ни разу не промахнувшись
   пальцем.

---

## 12. По фазам

**8.2 — чтение.** Шапка с переключателем месяца, итог, список по дням,
пустое состояние, скелеты, экран истёкшей сессии. Графиков ещё нет.

**8.3 — правки.** Карточка операции, добавление, удаление с возвратом,
оптимистичное сохранение, чужие записи только для чтения.

**8.4 — картина.** Спарклайн, две полосы участников, категории, дни,
фильтры из графиков в список, блок «не разобрано».
