# Дизайн веб-интерфейса

Дополнение к `webapp.md`: что именно на экранах и почему оно выглядит так.
`webapp.md` описывает механику (вход, API, права), этот документ — то, что
видит человек.

Реализовывать по фазам 8.2–8.4. Не выдумывать экранов сверх описанных.

---

## 1. Кому и зачем

Двое, общий бюджет. Записывают тратами боту с телефона, на бегу. Сайт
открывают тоже с телефона — значит, **мобильный экран первичен**, десктоп
получается растяжением, а не наоборот.

Задачи, по убыванию частоты:

1. **«Сколько ушло и на что»** — понять за пять секунд после открытия.
2. **«Исправить вот эту запись»** — опечатка в сумме, не та категория, не тот
   день. В боте сумму не исправить вообще: ради этого сайт и затевается.
3. **«Проверить, что бот напридумывал»** — записи, которым категорию поставил
   не человек, а модель вслепую. У бота такого экрана нет.
4. **«Кто из нас сколько платит»** — не для взаиморасчётов (их не считаем,
   `plan.md` §10), а чтобы видеть перекос.
5. **«Добавить руками»** — задним числом, без бота.

Всё остальное — не задача. Ни бюджетов по категориям, ни целей, ни прогнозов.

### 1.1 Что каждая задача требует от интерфейса

| Задача | Чем закрывается |
|---|---|
| 1 | Итог месяца, дельта, категории, полосы участников (§3.2–3.5) |
| 2 | **Поиск** и фильтры (§3.2), список (§3.9), карточка (§4) |
| 3 | Блок «Проверить» (§3.8) и быстрый разбор пачкой (§4.3) |
| 4 | Полосы «кто платил / на кого ушло» (§3.6) |
| 5 | Кнопка «+» и карточка с произвольной датой (§4) |

Проверка простая: если задача не закрывается перечисленным — виноват
интерфейс, а не человек.

---

## 2. Экраны и переходы

| Хэш | Что |
|---|---|
| `#/m/2026-07` | Месяц: сводка, графики, список операций |
| `#/m/2026-07/t/482` | То же плюс открытая карточка операции |
| `#/m/2026-07/new` | То же плюс форма добавления |
| `#/search?q=пятёрочка` | Поиск по всем месяцам |

Карточка — не отдельная страница, а слой поверх месяца: на телефоне лист
снизу, на десктопе модалка шириной до 560px. Кнопка «назад» закрывает лист,
а не уводит с месяца.

**История браузера.** Смена месяца — `replaceState` (иначе «назад» после
пяти листаний возвращает по одному месяцу), открытие листа и фильтр —
`pushState`.

Фильтры живут в хэше: `#/m/2026-07?cat=3&payer=1&pending=1`. Категория —
**идентификатором, не именем**: кириллица в URL требует кодирования и
ломается при переименовании. Отфильтрованный вид пересылается ссылкой.

Никаких вкладок, бургеров и нижней навигации: экранов по сути два.

---

## 3. Экран месяца, блок за блоком

Одна вертикальная лента. Сверху — то, ради чего открыли, ниже — детали.
Ширина контента ограничена 720px и центрируется.

### 3.1 Шапка (прилипает)

```
‹     Июль 2026     ›     🔍
```

При скролле сжимается до 44px: слева «Июль», справа итог мелким. Свайп
влево-вправо листает месяц, но **только вне 20 пикселей от краёв экрана** —
там живёт системный жест «назад». Будущие месяцы недоступны, стрелка «›»
гаснет на текущем.

Липкая шапка (44px) и липкий заголовок дня (32px) вместе съедают 76px —
это предел, больше ничего к верху не липнет.

### 3.2 Поиск и фильтры

Задача 2 без поиска не закрывается: пролистать до записи трёхмесячной
давности — это полсотни экранов вслепую.

Лупа в шапке разворачивает строку поиска. Запрос уходит на сервер и ищет
**по описанию и по сумме сразу по всем месяцам**: «пятёрочка» найдёт по
описанию, «1200» — по сумме. Результаты — тот же список, сгруппированный по
дням, с указанием месяца в заголовке дня; тап открывает карточку.

Ниже шапки — строка активных фильтров, если они есть:

```
Продукты ✕    Илья ✕
```

Фильтры: категория, плательщик, «на проверку». Все они — **только для
списка**. Итог, дельта, спарклайн, полосы и график по дням всегда считаются
за весь месяц: дельта «↑ 6% к июню» под фильтром по категории не имеет
смысла, а два разных набора цифр на одном экране путают сильнее, чем
помогают.

### 3.3 Итог месяца — главное число

Одно на весь экран, ≥48px, тот же системный шрифт, что везде, цифры
пропорциональные.

```
Всего за июль
68 400 ₽
↑ 6% к июню · за первые 28 дней
```

**В итог входят только расходы.** Переводы между партнёрами и поступления
исключены — ровно как в `/месяц` у бота (`plan.md` §3, §10). Иначе цифра на
сайте разойдётся с цифрой в чате, и доверия к обеим не будет.

**Сравнение честное.** Неполный месяц сравнивается с тем же отрезком
прошлого. Правила:

- N = min(прошедших дней текущего месяца, дней в прошлом месяце). Иначе 31
  июля сравнивалось бы с несуществующим 31 июня.
- Подпись показывает фактический N: «за первые 28 дней».
- **При N ≤ 3 дельта не показывается вовсе.** Два дня против двух дней дают
  разброс в сотни процентов — это не информация.
- Обе цифры считает сервер и отдаёт одним ответом (§10). Складывать их на
  клиенте нельзя: разойдётся с ботом.

**Дельта нейтральная.** Стрелка и знак несут смысл, цвет — нет. Красить рост
трат в тревожный красный каждый месяц значит превратить учёт в надзирателя;
у роста бывает причина — отпуск, ремонт, зубной.

При нулевом итоге дельта и спарклайн не рисуются: сравнивать нечего.

### 3.4 Шесть месяцев подряд

Спарклайн под итогом. **Шесть завершённых месяцев, текущий не входит** —
третьего числа он выглядел бы обвалом, а §3.3 двумя абзацами выше объясняет,
почему так делать нельзя. Тап по точке уводит на тот месяц: это же и
единственный способ прыгнуть далеко назад, минуя стрелки.

Осей и подписей нет — это форма, а не таблица.

### 3.5 По категориям

Горизонтальные бары, отсортированы по убыванию. **Все бары одного цвета** —
это одна серия, и цвет здесь не значит ничего, кроме «это данные». Раскрасить
14 категорий в 14 оттенков — классическая ошибка: после седьмого цвета
соседние перестают различаться, а под дальтонизмом ломаются раньше.

```
Продукты          ████████████████  22 100 ₽  32%
Жильё             ████████████      18 000 ₽  26%
Доставка еды      ██████             9 300 ₽  14%
…
Без категории     ██                 1 400 ₽   2%
                  Ещё 6 категорий ▾
```

- Восемь строк, хвост сворачивается в «**Ещё N категорий**» с раскрытием.
  Не «Остальное»: в таблице есть настоящая категория «Прочее», и две похожие
  строки рядом — гарантированная путаница.
- Строка «Без категории» показывается **всегда отдельно и вне свёртки**, даже
  если она мелкая: это не категория, а работа, которую надо доделать.
- Толщина бара ≤24px, конец данных скруглён на 4px, у базовой линии — прямой.
- Значение у кончика бара, процент следом приглушённым.
- Тап по строке фильтрует список ниже (пишется в хэш).

### 3.6 Кто платил и на кого ушло

Две полосы 100%-стека, одна под другой, **подписи под полосой, а не внутри**:

```
Кто платил
[■■■■■■■■■■■■■■■■■■][■■■■■■■■]
● Илья 47 200 ₽   ● Аня 21 200 ₽

На кого ушло
[■■■■■■■■][■■■■■][■■■■■■■]
● Илья 31 500 ₽   ● Аня 18 900 ₽   ● Общее 18 000 ₽
```

Подпись внутри сегмента на 360px не помещается: полоса шириной 328px, сегмент
в 15% — это 49px, куда «Партнёру 6 800 ₽» не влезет никогда, а обрезать
подписи запрещено. Поэтому легенда под полосой — она и так обязана быть.

**Цвет закреплён за человеком навсегда** и назначается по ключу из
`/api/me`: тот, кто смотрит, — слот 1, партнёр — слот 2. Не по порядку в
базе: иначе у двоих будут разные цвета у одних и тех же людей.

Служебные корзины — слот 3: «Общее» (траты на двоих) и «Партнёру» (партнёр
боту ещё не писал, `report.go`). «Кто-то ещё» — нейтральная заливка. Они не
могут встретиться в одной полосе одновременно с разным смыслом: «Партнёру»
существует, только пока второго участника нет, а тогда нет и «Общего»
пополам делить не с кем — но если встретятся, у каждой своя подпись в
легенде, и цвет не остаётся единственным различием.

Сегменты разделяет **зазор 2px цветом фона**, не обводка.

### 3.7 По дням

Столбики на всю ширину месяца. Одна серия — один цвет; сегодняшний столбик
акцентный. Подписи оси только 1, 8, 15, 22, 29.

**Тапа по столбику нет.** 31 столбик в 328px — это 10.6px на шаг, палец
накрывает четыре сразу, а §5.4 требует цели не меньше 44px. График здесь
обзорный: увидеть всплеск. Числа доступны в раскрывающемся списке дней под
графиком и в самом списке операций ниже.

На десктопе, где есть курсор, при наведении показывается подсказка: день,
сумма, число операций.

### 3.8 «Проверить»

```
⚠ 4 записи разобраны вслепую          Проверить →
```

Смысл блока не тот, что кажется. Флаг `needs_classification` живёт минуты:
воркер добора разбирает такие записи каждые десять минут, а через два часа
принудительно закрывает категорией «Прочее» (`worker/backfill.go`). То есть
записи, которым **действительно нужен человек**, — это не «без категории», а
те, которым категорию поставили вслепую, и в списке они неотличимы от
честно выбранного «Прочего».

Поэтому: миграция добавляет `needs_review boolean not null default false`;
воркер, закрывая запись «Прочим» без разбора, ставит его; любая ручная
правка снимает. Блок показывает именно их и появляется, только если такие
есть.

Это единственное место, где интерфейс что-то требует, — поэтому оно и
выглядит иначе: статусный цвет плюс значок плюс слова. Цвет один смысла не
несёт никогда.

### 3.9 Список операций

Сгруппирован по дням, свежие сверху. **Группировку по дням и месяцам делает
сервер** в таймзоне бота и отдаёт готовое поле `day`: `spent_at` в базе — UTC,
и если группировать в браузере, трата, записанная в 00:30 по Москве, у
телефона в другой зоне уедет во вчера, а граница месяца сдвинется.

```
Сегодня                              расходы 1 600 ₽
  Пятёрочка                                1 200 ₽
  Продукты · на двоих                          ИБ
  Такси                                      400 ₽
  Такси · на себя                              А
```

- Сумма в заголовке дня — **расходы за день**, с явной подписью. Переводы и
  поступления в неё не входят, как и в итог месяца.
- Высота строки 56px — палец попадает без прицеливания.
- Слева описание крупно, под ним категория и «на кого» приглушённым.
- Справа сумма табличными цифрами, чтобы разряды стояли колонкой, под ней
  кружок с инициалом плательщика **в цвете его слота**. При совпадении первых
  букв («Илья» и «Ирина») берутся две буквы.
- Запись на проверку — тонкая полоска статусного цвета слева.
- Перевод — «↔ Перевод», сумма приглушённая: в итог он не входит и выглядеть
  как трата не должен.
- Поступление — знак «+» перед суммой.

Тап по строке открывает карточку. **Свайпа-удаления нет**: смахнуть деньги
случайно нельзя.

**Объём.** Месяц грузится страницами по 200 записей, дальше — «Показать
ещё». Шестьсот строк в DOM с липкими заголовками на слабом телефоне
подтормаживают, а виртуализация ради двух человек — лишняя сложность.

Нижний отступ ленты — 56 + 16 + `env(safe-area-inset-bottom)`, иначе кнопка
«+» ляжет на сумму последней операции.

### 3.10 Подвал

```
Илья · Выйти · Выйти отовсюду
За месяц 840 000 токенов из 2 000 000
```

Единственное место для выхода из сессии (`webapp.md` §1) и для расхода
токенов. Вкладок и меню нет, деть эти две строки больше некуда, а без
кнопки «выйти» модель безопасности неполна.

### 3.11 Кнопка «добавить»

Круглая, 56px, в правом нижнем углу, над безопасной зоной. Одна на весь
интерфейс — потому что действие ровно одно.

---

## 4. Карточка операции

На телефоне — лист снизу с ручкой, на десктопе — модалка. Открывается за
150мс, закрывается свайпом вниз, «назад» или крестиком. Лист прокручивается:
всё содержимое в один экран телефона не помещается и не должно.

```
─────
Трата

    1 200 ₽              ← крупное поле, цифровая клавиатура

Описание   [ пятёрочка            ]

Категория
[ Продукты        ] [ Кафе и рестораны  ]
[ Доставка еды    ] [ Транспорт         ]
[ Такси           ] [ Жильё             ]
…

На кого   [ 👤 мне ][ 🧍 ей ][ 👥 нам ]

Тип       [ трата ][ перевод ][ доход ]

Когда     [ сегодня ][ вчера ][ 📅 12.07.2026 ]

из сообщения: «вчера взял в пятёрочке на 1200 и такси 400»
исправлено 28.07 в 14:20

[        Сохранить        ]
              Удалить
```

- **Сумма — первое и самое крупное поле.** Ради неё сюда и приходят. Но
  **автофокуса нет**: клавиатура закрыла бы весь выбор категории.
- **Категории — чипы в две колонки, с полными именами из таблицы
  `categories`.** Сокращать нельзя: `plan.md` §6 требует, чтобы список нигде
  не расходился с базой. «Хобби и развлечения» на 16px занимает ~180px — в
  две колонки на 360px это семь рядов, лист прокручивается.
- **Дата — три чипа плюс нативный `input type="date"`.** Без него нельзя ни
  перенести трату на 8-е, ни завести запись за прошлую неделю, а это прямо
  задачи 2 и 5. Ограничения — из `webapp.md` §4: не дальше суток вперёд, не
  раньше 2020 года. В форме добавления дата по умолчанию — сегодня, если
  открыт текущий месяц, иначе первое число открытого.
- **Тип «перевод»** прячет категорию и «на кого»: у перевода их не бывает
  (`plan.md` §3). Поля исчезают, а не гаснут.
- **Исходное сообщение** внизу серым — объясняет, откуда взялась сумма.
  Рядом отметка о правке, если запись правили (`updated_at`).
- **Чужая запись** — те же поля, но неактивные, сверху плашка «Запись Ани».
  Не прятать: смотреть чужие траты можно, бюджет общий.

### 4.1 Сохранение

Оптимистичное: список обновляется сразу, при ошибке откатывается и
показывает тост с причиной.

**Проверка версии обязательна.** Запись могут менять одновременно с трёх
сторон: сайт, инлайн-клавиатура под старым сообщением в боте и воркер
добора, который ходит в модель секундами и потом пишет `category_id`,
`beneficiary`, `kind` и `spent_at`. Без проверки правка человека молча
затирается догадкой модели, причём PATCH вернёт 200 и интерфейс покажет
неправду.

Поэтому PATCH несёт `updated_at`, который был у клиента; сервер сверяет и при
расхождении отвечает 409 с новым состоянием записи. Интерфейс показывает:
«Запись изменилась» и что именно, с выбором «оставить моё» или «взять новое».

Смена категории на сайте снимает `needs_classification` и `needs_review` —
иначе запись вернётся к воркеру и он её переразберёт.

### 4.2 Удаление

Подтверждение, потом тост «Удалено · Вернуть» на шесть секунд. Удаление
мягкое, возврат — тот же PATCH, страх нажать кнопку исчезает.

### 4.3 Разбор пачкой

Задача 3 — это не одна запись, а несколько подряд. Через карточку каждая
стоит трёх тапов плюс две анимации; в боте — двух тапов без прокрутки. Так
не пойдёт.

Поэтому в режиме «Проверить» строка списка раскрывается **на месте**: тап по
строке разворачивает под ней сетку категорий, тап по категории сохраняет и
сворачивает. Два тапа, без листа и без прыжка списка.

Разобранная запись **остаётся на месте**, помеченная как готовая, пока не
уйдёшь с фильтра. Иначе список прыгает под пальцем и следующий тап попадает
не туда.

---

## 5. Как это выглядит

### 5.1 Поверхности и текст

Тёмная тема основная — открывать это будут вечером с телефона. Светлая
обязательна и собирается из тех же ролей, а не инверсией.

| Роль | Тёмная | Светлая |
|---|---|---|
| Фон страницы | `#0d0d0d` | `#f9f9f7` |
| Карточка, лента | `#1a1a19` | `#fcfcfb` |
| Поднятый слой (лист, модалка) | `#232322` | `#ffffff` |
| Волосяная линия | `rgba(255,255,255,.10)` | `rgba(11,11,11,.10)` |
| Основной текст | `#ffffff` | `#0b0b0b` |
| Вторичный текст | `#c3c2b7` | `#52514e` |
| Приглушённый (оси, подписи) | `#898781` | `#6b6a66` |

Приглушённый в светлой теме **свой**: `#898781` на светлом фоне даёт 3.4:1,
а §8 требует 4.5:1 для текста — и именно им набраны категория и «на кого».
`#6b6a66` даёт ~5:1.

Чистый чёрный не используем: на OLED он даёт смазывание при скролле и
проваливает границы карточек.

### 5.2 Цвет данных

Три слота, больше не нужно — и это проверено, а не на глаз:

| Слот | Кто | Тёмная | Светлая |
|---|---|---|---|
| 1 | смотрящий, все одиночные графики, акцент | `#3987e5` | `#2a78d6` |
| 2 | партнёр | `#d95926` | `#eb6834` |
| 3 | «Общее», «Партнёру» | `#199e70` | `#1baf7a` |

Прогнано `tools/validate_palette.js` в обоих режимах на всех парах: диапазон
светлоты, порог насыщенности, различимость при дальтонизме (худшая пара
ΔE 9.2 светлая / 9.4 тёмная при пороге 8) и контраст к фону — всё зелёное.
Единственное замечание: в светлой теме слот 3 даёт контраст 2.74 к фону,
поэтому у полос **всегда есть подписи в легенде** — цвет не остаётся
единственным носителем смысла. Скрипт лежит в репозитории, прогон входит в
приёмку (§11).

Статусные цвета — `#fab219` (проверить) и `#d03b3b` (удаление, ошибка).
Только для состояний, никогда как серия, и всегда со значком и словами.
Кнопка «Удалить» — текстовая, в критическом цвете: это разрушающее действие,
и оно обязано выглядеть иначе, чем «Сохранить».

### 5.3 Шрифт и цифры

Системный `system-ui, -apple-system, "Segoe UI", sans-serif` — ничего не
качаем, значит нечему тормозить.

| Что | Размер / вес |
|---|---|
| Итог месяца | 48 / 600, пропорциональные цифры |
| Заголовок экрана, суммы дня | 20 / 600 |
| Строка списка, поля | 16 / 500 |
| Категория, «на кого», подписи | 13 / 400 |
| Оси графиков | 12 / 400 |

Табличные цифры — в колонке сумм и на осях. На крупных числах их не
включаем: они разъезжаются.

**Капса нет нигде.** Кириллица в верхнем регистре читается заметно хуже
латиницы, а «ПРОДУКТЫ» ещё и кричит.

### 5.4 Размеры и ритм

Шкала 4 / 8 / 12 / 16 / 24 / 32 / 48. Поля экрана 16 на телефоне, 24 на
десктопе. Скругления: 16 у карточек, 12 у полей и кнопок, полное у чипов.
Тач-цели не меньше 44×44, кнопки в листе — 48 в высоту. Нижний отступ
уважает `env(safe-area-inset-bottom)`.

Теней в тёмной теме нет — их там не видно; слои разделяются светлотой
поверхности и волосяной линией. В светлой поднятый слой получает мягкую тень.

### 5.5 Компоненты

Без этой таблицы двое сделают разные макеты.

| Компонент | Размеры | Обычное | Активное / выбранное | Выключенное |
|---|---|---|---|---|
| Кнопка основная | h48, r12, 16/600 | фон слот 1, текст белый | яркость −8% | фон линии, текст приглушённый |
| Кнопка текстовая | h44, 16/500 | текст слот 1 | подложка 8% | текст приглушённый |
| Кнопка разрушающая | h44, 16/500 | текст `#d03b3b` | подложка 8% | — |
| Чип категории | h40, r999, 15/500 | фон поверхности, линия | фон слот 1, текст белый | — |
| Сегментированный | h40, r12 | фон поверхности | сегмент в слот 1 | — |
| Поле ввода | h48, r12, 16/400 | линия 1px | линия слот 1, 2px | фон линии |
| Поле с ошибкой | — | линия `#d03b3b` | + подпись под полем | — |
| Строка списка | h56 | — | подложка 6% при нажатии | — |
| Тост | h48, r12, снизу 16 | поднятый слой + линия | — | — |
| Плашка «чужая» | h36, r8 | фон 8% статуса | — | — |
| Шапка сжатая | h44 | фон страницы + линия снизу | — | — |
| FAB | 56, круг | слот 1 | яркость −8% | — |

### 5.6 Движение

Только два вида: появление листа снизу (200мс) и смена содержимого при
переключении месяца (150мс, растворение). Всё — `ease-out`.
`prefers-reduced-motion` выключает оба.

Чего не делаем: анимированного счёта цифр, параллакса, пружин, скелетонов,
мигающих при каждом перезапросе.

---

## 6. Правила графиков

Единые для всех трёх, чтобы они читались как один набор:

- **Толщина марок:** бар ≤24px, линия 2px, точка ≥8px.
- **Сетка и оси:** сплошные волосяные линии на шаг от фона. Пунктира нет —
  он читается как «прогноз».
- **Разделение:** зазор 2px цветом фона между сегментами стека и соседними
  барами. Обводок вокруг марок нет.
- **Подписи выборочные:** значение у кончика бара. Число над каждой точкой —
  шум, который не читают.
- **Текст никогда не красится в цвет данных.** Опознание даёт цветная метка
  рядом, а не окрашенная надпись.
- **Подсказка при наведении — только на десктопе.** На телефоне наведения
  нет, поэтому ни одно число не живёт исключительно в подсказке: категории
  подписаны, дни раскрываются списком, операции есть ниже.
- **Таблица-двойник:** «по категориям» и так таблица; у графика по дням
  раскрывается список дней с суммами.

Чего не будет: пончиков и круговых диаграмм (сравнивать доли по секторам
люди не умеют), двух осей Y, радуги вместо шкалы, 3D.

---

## 7. Состояния

| Состояние | Что показываем |
|---|---|
| Первая загрузка | Скелет блоков — форма будущего экрана, не спиннер |
| Смена месяца | Прошлый экран приглушается до 60%, layout не прыгает |
| Пустой месяц | «В июле трат нет» и формат: «600 лимонад». Дельты и спарклайна нет |
| Нет связи | Плашка сверху: «Сервер не отвечает · Повторить» |
| Сессия истекла (401) | Экран «Сессия закончилась. Напиши боту /вход» |
| Запись изменилась (409) | «Запись изменилась» + выбор: оставить моё / взять новое |
| Записи больше нет (404) | «Этой записи больше нет», карточка закрывается |
| Ошибка сохранения | Откат значения и тост с причиной |
| Чужая запись | Плашка «Запись Ани», поля неактивны |
| Поиск без результата | «Ничего не нашлось по „кофе“» |

---

## 8. Доступность

- Контраст текста ≥4.5:1 в обеих темах; крупного — ≥3:1.
- Смысл никогда не только в цвете: у статуса значок и слово, у сегментов
  графика подпись в легенде, у «на кого» иконка и текст.
- Фокус виден на всём, что кликается; Tab обходит экран в порядке чтения.
- Лист снизу — ловушка фокуса, Esc закрывает.
- `prefers-reduced-motion` уважается.
- Никаких подсказок только по наведению.

---

## 9. Чем и как это писать

React 18 + Vite + TypeScript, как решено. Сверх этого:

- **Графиков-библиотек нет.** Три графика — бары, стек, спарклайн — это
  40–60 строк SVG каждый. `recharts` добавил бы ~120 КБ и всё равно
  потребовал бы переопределять каждый стиль под §6. Это осознанная замена
  решения из `webapp.md` §5, тот документ поправлен.
- **CSS-фреймворка нет.** Переменные из §5 в одном файле, дальше обычный
  CSS-модуль на компонент.
- **Менеджера состояния нет.** `useState` плюс тонкая обёртка над `fetch`,
  которая умеет 401 (увести на экран сессии), 409 (показать расхождение) и
  откат.
- **Роутер свой** — тридцать строк на разбор хэша.

Бандл-потолок: **60 КБ gzip** без React, 110 КБ с ним.

---

## 10. Что API должен уметь сверх `webapp.md` §4

- `GET /api/report/month` возвращает **и текущий период, и сопоставимый
  отрезок прошлого месяца** (§3.3), чтобы дельту не считали на клиенте.
- `GET /api/report/months?n=6` — итоги шести **завершённых** месяцев.
- `GET /api/transactions` принимает `pending=1` (записи на проверку),
  `payer`, `category` (идентификатором), `limit`/`offset`, и отдаёт готовое
  поле `day` в таймзоне бота плюс счётчики `total` и `pending`.
- `GET /api/search?q=` — по описанию и сумме, по всем месяцам.
- `PATCH /api/transactions/{id}` принимает `updated_at` клиента и отвечает
  409 при расхождении; понимает `{"deleted": false}` для возврата удалённого
  (отдельного вложенного эндпоинта не заводим — `webapp.md` §4 запрещает
  вложенность).
- Миграция: `transactions.needs_review boolean not null default false`;
  воркер ставит его, закрывая запись «Прочим» вслепую; правка снимает.
- `worker.ApplyClassification` дополняется условием `and needs_classification`
  — чтобы не затирать то, что человек уже поправил руками.

---

## 11. Приёмка

Фаза не закрыта, пока не проверено руками:

1. Ширина окна 360px: горизонтального скролла нет, ничего не обрезано.
2. Суммы в списке стоят колонкой, разряд под разрядом.
3. Переключение месяца не дёргает layout и не мигает белым.
4. Светлая тема не «инверсия»: контрасты держатся, графики читаются.
5. `node tools/validate_palette.js` зелёный в обоих режимах.
6. `prefers-reduced-motion` гасит анимации.
7. Клавиатура: Tab доходит до всех действий, фокус виден, Esc закрывает лист.
8. Бандл в пределах §9.
9. **Итог месяца, категории и «кто платил» совпадают с `/месяц` в боте**
   за тот же месяц, до рубля.
10. Месяц с 600 записями открывается и листается без рывков.
11. Открыть на телефоне и исправить трату, ни разу не промахнувшись пальцем.

---

## 12. По фазам

**8.2 — чтение.** Шапка с переключателем месяца, итог месяца с дельтой
(`GET /api/report/month` переезжает сюда из 8.4 — считать итог на клиенте
нельзя), список по дням с пагинацией, поиск, пустое состояние, скелеты,
экран истёкшей сессии, подвал с выходом.

**8.3 — правки.** Карточка операции, добавление, произвольная дата,
удаление с возвратом, проверка версии и экран расхождения, оптимистичное
сохранение, чужие записи только для чтения.

**8.4 — картина.** Спарклайн, полосы участников, категории, дни, фильтры,
блок «Проверить» с разбором пачкой (миграция `needs_review` и правка
воркера — сюда же).
